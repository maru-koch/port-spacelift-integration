description: spacelift integration for Port Ocean
icon: Cookiecutter  # Should be one of the available icons in Port
features:
  - type: exporter
    section: Under Development  # Should be one of the available sections in Port
    resources:
      - kind: spacelift-example-kind
      # - kind: <ResourceName2>
configurations:
  - name: myspaceliftToken
    # required: true
    type: string
    sensitive: true
  - name: someApplicationUrl
    type: url
